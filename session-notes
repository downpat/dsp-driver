session for 2026-4-26
  started @ 7:07am
  finished @ 8:20am

  created git repository for dsp driver app
  achieved first build of .class, .dex files

  next session
    design a proper build, test cycle
      ChatGPT does NOT know how to do this. Don't ask for it's help
    export PATH=$PATH:$ANDROID_SDK_ROOT/build-tools/34.0.0 needs to be added to the Dockerfile so aapt is available in the path

session for 2026-5-1
  started @ 8:33am
  finished @ 1:33pm

  updated dockerfile to include Android build tools in the path
  built the .dex file by starting with a jar file created from the package that contains the .class file
    the d8 command runs from the parent of the "com" directory, the package root
  built the "unaligned.apk" by updating the "aapt" command to use the directory structure for the package
    main problem was trying to include AndroidManifext.xml in the package when the -M argument already identifies it
      this was causing a duplicate file error
  updated "build/apk/unaligned.apk" with "classes.dex" using zip command
  aligned the apk using zipalign command
  created a debug keystore for signing apks
  built the first apk
  packaged all build commands into apk_build.sh
  debugged a path package issue with the apk
  launched the first version of the app on my phone
  explored the directory structure of the phone
    apps seem to be completely sandboxed
    credentials are stored in folders that need root access
    Flex doesn't seem to save anything at all to user-available files
    So if we want to see what Flex is doing on the file system, we'll have to root this phone
      We'd also have to pray that doesn't break any essential work functions - Flex, ADP, etc
    so if we want to authenticate with Amazon credentials, we'll probably need to incorporate that
    we already know that Amazon doesn't provide open access to the Flex API, but we also know that
      they're not exactly locking it down either
    So here's a list of things we need:
      A way to understand what data Flex is storing on the device
        Starts with root access
        Might not have any practical benefits because our app won't have access to these files
      A way to authenticate to the Flex API
      A way to understand what endpoints of the API are available and what data we can get from it
  brainstorming with Claude about capturing Flex network traffic
    Setup PCAPdroid on my phone, tried to login to Flex, "connection refused"
      Seems like Amazon is ready for the PCAPdroid approach, at least with the login, which is
        probably a whole different team at Amazon, and the Flex team is just using a library
        for it
    I logged in and restarted the PCAPdroid VPN, every connection is still refused. Seems like it's locked down pretty tight
    After stopping PCAPdroid, the app works fine, it's not just the Flex login
    We're going to have to try a mitm using the home router and a laptop first. And then, more than likely, we're
      going to have to do a field test with a router and laptop in the van with me - or maybe just a rooted burner
      phone. That's the only way we're going to get the data we need.
    One of the things I need to remind myself of is that even though Amazon's security features are locking me out
      of the development I want to do, I'm not actually doing anything nefarious. I'm actually trying to make their
      delivery drivers more efficient, and that helps them.

  todo
    need to make sure I'm leaving proper context for myself, I had to re-read the ChatGPT convo to remember what I was doing
      I am not an expert in Android development... yet. So I need to leave myself fuller context between sessions
    design the right command for building classes.dex
      build a jar file with "jar cf classes.jar" from the "build/obj" directory, it will pick up the packages there
        "dex" files can't be present or they will be compiled into the jar and you can't mix .dex and .class files in an Android apk
    create a build.sh or something like it that builds and installs the APK
    make build.sh the CMD for the Dockerfile
    run the Dockerfile with no extra command to generate a build and run the app on the phone
    investigate Flex directories on the phone
    strengthen the passwords on the keysign commands
    keystore should be generated in a bash file and added to the Dockerfile as part of the build, not the CMD, so the keystore creation
      is part of the Docker build

    next session
      setup home network and laptop to create a mitm proxy that will collect Flex network traffic
      design app architecture to separate concerns - data gathering modules should run on laptop
        as effectively as phone - be modular, you know how
      keystore should be generated in Dockerfile
        strengthen the passwords on the keysign commands
      update Dockerfile CMD to use apk_build.sh so that the docker run command builds an apk
        build steps should be
         docker run ...
         adb install path/to/apk
